/*StartupEvents.registry('item', (event) => {
const drone_type = ["harvest", "hunter", "miner"];
const drone_tier = ["basic", "advanced"];
for (const drone_tier of drone_type) {
    event.create('${drone_tier}_drone_${drone_type}').maxStackSize(64)
}
});*/
